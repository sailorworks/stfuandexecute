{
    "name": "stfu-contracts",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy:monad": "npx hardhat run scripts/deploy.ts --network monadTestnet"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.28.6"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.4.0",
        "dotenv": "^17.3.1"
    }
}
